import SubscriptionRequestController from './SubscriptionRequestController'

const Payment = {
    SubscriptionRequestController: Object.assign(SubscriptionRequestController, SubscriptionRequestController),
}

export default Payment